# Examples

Run default table audit:

```bash
brew-adoption-audit
```

Include `~/Applications`:

```bash
brew-adoption-audit --include-user-applications
```

Generate Markdown:

```bash
brew-adoption-audit --format markdown --output report.md
```

Generate JSON:

```bash
brew-adoption-audit --format json --output report.json
```
